import React from "react";

const Projects = () => {
  return (
    <pre className="text-blue-400">
      {`const projects = [`}
      <br />
      {`  { name: "YouTube Clone", stack: "React, Node.js, HLS.js" },`}
      <br />
      {`  { name: "Phonopedia", stack: "MERN, MongoDB Atlas" }`}
      <br />
      {`];`}
    </pre>
  );
};

export default Projects;
